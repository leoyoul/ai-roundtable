# 可选：固定 API Key 加密密钥。生产环境建议设置一个长随机字符串并妥善保存。
# 如果不设置，应用会在 data/secret.key 自动生成本机密钥。
AI_ROUNDTABLE_SECRET=

# 可选：SQLite 数据库路径。Docker 默认使用 /app/data/ai-roundtable.sqlite。
AI_ROUNDTABLE_DB_PATH=/app/data/ai-roundtable.sqlite
